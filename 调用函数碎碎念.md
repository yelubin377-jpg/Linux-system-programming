open()   open() 失败返回-1

write()

strtok() 分割字符串

pow() 计算次方

fopen() 打开文件

open() 系统调用

gnu_get_libc_version() 函数，返回字符串形式的版本号

fwrite() 写文件失败，返回 -1 +设置 errno

fopen() 打开文件失败，返回 NULL      “目标写错了”（ ENOENT ）或“内存超限了”（ ENOSPC ）。

lib/tlpi_hdr.h 头文件

  fflush

  fprintf

  vfprintf

exit

errExit() / errMsg() 

atoi() / strtol()

getLong()会检查空参数、非数字字符、数值范围、正负约束 

 getInt() 

特性测试宏（如 _POSIX_C_SOURCE ） #define _POSIX_C_SOURCE 200809L

 tlpi_hdr.h 头文件